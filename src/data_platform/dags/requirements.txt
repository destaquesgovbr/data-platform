# Dependências Python para DAGs do Cloud Composer
# Versões exatas para acelerar instalação (evita timeout no dependency resolution)

# HuggingFace dependencies for sync_postgres_to_huggingface DAG
# Nota: Removemos 'datasets' (pesado, causava OOM) - usamos API + parquet diretamente
huggingface-hub==0.27.0
pyarrow>=14.0.0
requests>=2.31.0

# Scraper DAGs (chamam Cloud Run API via HTTP, não executam scraper localmente)
httpx>=0.27.0
google-auth>=2.29.0

# Config parsing (usado no parse time para gerar DAGs dinâmicas)
pyyaml>=6.0.2
